<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sell">

    <application
        android:name=".util.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".fragment.CommodAllFragment" />
        <activity android:name=".view.UpDateZFBActivity" />
        <activity android:name=".view.CommodCustomerActivity" />
        <activity android:name=".view.CommodReleaseActivity" />
        <activity android:name=".view.CommodClassActivity" />
        <activity android:name=".view.CommodCommodActivity" />
        <activity android:name=".view.CommodShopActivity" />
        <activity android:name=".activity.RegisterAllActivity" />
        <activity android:name=".activity.MySheZhiActivity" />
        <activity android:name=".view.MyZFBActivity" />
        <activity android:name=".activity.ShowActivity" />
        <activity android:name=".activity.ForgetActivity" />
        <activity android:name=".activity.RegisterActivity" />
        <activity
            android:name=".activity.MainActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>